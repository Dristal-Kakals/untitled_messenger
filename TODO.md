- [x] исправить ошибки при сборке — `Event::HistoryLoaded` стал struct variant `{ chat, msgs, has_more }`, а match-arm в `app::handle_event` ещё ссылался на tuple-форму `HistoryLoaded(chat, msgs)` → E0164. Arm переписан под struct-поля + добавлена обработка `Event::OlderHistoryLoaded` (prepend страницы к кэшу, обновление keyset-курсора, очистка in-flight маркера). Сборка чистая, без warning'ов (предыдущий dead-code на `ChatScrolled` закрыт подключением `on_scroll` в обоих scrollable). Выполнено 2026-07-02.
- [x] обернуть постквантовой криптографией всё что шифруется — ML-KEM-768 hybrid PQXDH поверх X3DH (1:1). Группы (Sender Keys, симметричный AEAD) получают PQ-защиту опосредованно: distribution state едет по гибридному 1:1 ratchet. Выполнено 2026-07-02.
- [x] GUI: production-рефакторинг — единая тема (`um_gui::theme`, Dracula), bubble/panel/button стили, авто-скролл с snap-to-end, временные метки UTC на сообщениях, card-лейауты setup/login/settings, статус-хедер, pre-flight проверка дисплея (чистый exit без паники winit на headless). Выполнено 2026-07-02.
- [x] GUI: групповые входящие сообщения показывают автора (ник контакта или short-hex) — `MessageView::sender` для живых group-сообщений, prefix "nick: " в group_chat. Выполнено 2026-07-02.
- [x] документация: README актуализирован — раздел про hybrid PQXDH (ML-KEM-768 поверх X3DH), обновлён раздел GUI features (тема, таймстемпы, автор групповых сообщений, headless-safe startup). Выполнено 2026-07-02.
- [x] GUI: persisted store хранит per-message sender — добавлен plaintext-столбец `sender BLOB` в `messages` (public metadata, pub-ключ автора как `peer`; НЕ внутри sealed blob, НЕ часть AEAD AAD — поэтому seal существующих rows не инвалидируется). `Store::put_message` принимает `Option<&[u8;32]>`, `StoredMessageRow` получил `sender: Option<[u8;32]>`. Миграция `ALTER TABLE messages ADD COLUMN sender BLOB` (идемпотентна, backfill `NULL`). Bridge: `deliver_chat` пишет `sender` для входящих group, `None` для 1:1/outgoing; `load_history` читает `r.sender` — после рестарта групповая история сохраняет метки автора. 2 новых store-теста (round-trip с sender + миграция старого store без столбца). Выполнено 2026-07-02.
- [x] GUI: двухколоночный лейаут — persistent sidebar (`views::contact_list::sidebar`, фиксированная ширина 300px, `sidebar_style`) слева + активная панель справа для post-login роутов (ChatThread, GroupChat, Settings). `app::view` строит `row![sidebar, right]` через хелпер `split`; `View::ContactList` = sidebar + placeholder ("select a chat"). Открытый чат подсвечен в сайдбаре (`active_row_style`, accent-tinted). Полная ширина окна 900px теперь используется (раньше 520px). `theme` получил `SIDEBAR_WIDTH`, `sidebar_style`, `active_row_style`. 4 новых app-теста (Back чистит open_chat, OpenGroup ставит open_chat, OpenSettings не чистит open_chat) + 3 theme-теста. Setup/Login остаются full-window карточками. Выполнено 2026-07-02.
- [ ] GUI: sidebar не сворачивается/не перетаскивается — фиксированная 300px. Resizable splitter (pane_grid) или collapse-button был бы следующим шагом, но это крупное UI-изменение.
- [x] GUI: search/filter box в сайдбаре — фильтрует и контакты, и группы по нику/имени группы/нижнему hex id (case-insensitive substring). `UmApp::search_query` + `Message::SearchChanged`; чистый предикат `views::matches_query` (empty/whitespace = show all, trim+lowercase substring по массиву haystack). Поле поиска под хедером, над скроллящимся списком; "No contacts/groups match your search" когда список непустой, но фильтр всё отрезал. `search_query` чистится на Logout. 5 тестов `matches_query` + 2 app-теста (SearchChanged updates, logout clears query). Выполнено 2026-07-02.
- [ ] PQ: group sender-key distribution едет по гибридному 1:1 ratchet опосредованно — прямой PQ-KEM для групп (MLS-подобный) вне scope v1.
- [x] PQ: интеграционный E2E-тест через реальный TCP (`e2e_hybrid_pqxdh_over_tcp`) — гибрид проверен через весь стек net→relay→session. Выполнено 2026-07-02.
- [x] PQ: сервер пропускает PQ-поля через Register/Bundle fetch (opaque `Vec<u8>`, serde) — подтверждено ассертами в e2e-тестах. Выполнено 2026-07-02.
- [x] GUI: ranked fuzzy search в сайдбаре — `views::query_rank` (pure) возвращает `Option<u32>` score: tier 0 exact / tier 1 prefix / tier 2 substring, +position внутри tier, +500 penalty для hex-id haystack (≥8 chars all hex/`…`) чтобы nickname-матчи всплывали выше pasted-pubkey матчей. `matches_query` теперь тонкая обёртка `query_rank().is_some()` (поведение идентично, старые тесты зелёные). `views::contact_list::sidebar` собирает `(rank, &ContactView)` / `(rank, &GroupView)`, `sort_by_key` (stable) — лучшие матчи наверху, при пустом запросе rank 0 для всех → сохраняется исходный порядок вставки. 7 новых `query_rank`-тестов (empty=0, no-match=None, exact<prefix<substring, earlier-pos wins, name beats hex-id, min-across-haystacks, short-hex treated as id). 245 тестов зелёных. Выполнено 2026-07-02.
- [x] GUI: подсветка совпадающего фрагмента в сайдбаре — `views::match_range` (pure) возвращает `Option<(usize, usize)>` байтовый диапазон первого case-insensitive совпадения trimmed-запроса в haystack (empty/whitespace → `None` = без подсветки, консистентно с `query_rank` "empty shows all"). `views::highlighted_name` строит `Vec<Span>`: before/match/after, matched-фрагмент через `span(..).color(HIGHLIGHT_FG).background(HIGHLIGHT)` (accent wash + тёмный текст для контраста). `contact_list::sidebar` рендерит nickname/group-name через `rich_text(highlighted_name(..))` вместо plain `text`; hex-id и fingerprint sub-линии остаются plain (подсветка там бесполезна). `theme` получил `HIGHLIGHT` (translucent accent, a<1) + `HIGHLIGHT_FG` (тёмный). 7 новых `match_range`-тестов (empty=None, no-match=None, case-insensitive substring, first-occurrence, trim, preserves original case range) + 2 theme-теста (highlight translucent, fg dark). 259 тестов зелёных. Выполнено 2026-07-02.
- [x] GUI: total-unread badge в хедере сайдбара — `UmApp::total_unread()` суммирует все значения `unread` (peers + groups); хедер `views::contact_list::sidebar` показывает `"{n} unread"` (accent) между статусом и кнопкой settings, либо пустой spacer той же высоты (без прыжков лейаута). Виден всегда, даже когда список проскроллен или отфильтрован. 3 новых app-теста: сумма across peer+group, drop при открытии чата, 0 после logout. Выполнено 2026-07-02.
- [x] store: индекс для load-history — закрыт индексом `idx_messages_peer_id (peer, id)` (см. ниже): `id` монотонен с порядком вставки = порядком доставки, поэтому `(peer, id)` покрывает и ORDER BY timestamp-семантику без отдельного `(peer, timestamp)` индекса. Выполнено 2026-07-02.
- [x] store: добавлен индекс `idx_messages_peer_id ON messages (peer, id)` — покрывает запрос `load_history` (`WHERE peer = ?1 ORDER BY id ASC`): planner делает index range scan вместо full table scan + sort. `(peer, id)` выбран вместо `(peer, timestamp)`, т.к. реальный `ORDER BY` ключ — `id` (монотонный с порядком вставки = порядком доставки). `CREATE INDEX IF NOT EXISTS` идемпотентен на reopen. Тест `messages_peer_id_index_exists_and_is_used`: проверяет наличие индекса в `sqlite_master`, использование его в `EXPLAIN QUERY PLAN` load-history-запроса, выживание после reopen. Выполнено 2026-07-02.
- [x] store: keyset pagination истории — `Store::messages_page(peer, before_id: Option<i64>, limit)` ходит по индексу `(peer, id)` reverse range scan'ом: `before_id=None` → newest N, `Some(id)` → N строк строго старше `id`. Фетчит `limit+1` probe-row чтобы отличить "нет больше истории" от "пустая страница" без второго count-запроса; probe дропается, `has_more=true` если он был. Возвращает `MessagePage { rows, has_more }`, rows oldest-first (готовы prepend'нуть к кэшу). Декодирование (unseal+AAD+sender+skip-corrupt) вынесено в общую `decode_message_rows` + `map_message_row` (free fn, не closure — чтобы `MappedRows`-типы `messages` и `messages_page` были совместимы). `Store::messages` оставлен как thin wrapper для back-compat. 8 новых store-тестов: newest-page oldest-first + has_more, before_id paging, union страниц = весь тред (без gap/dup), peer-изоляция, пустой тред, limit=0 probe-семантика, sender metadata round-trip через paged path, EXPLAIN QUERY PLAN использует индекс (не full scan). Выполнено 2026-07-02.
- [x] GUI: scroll-to-top подгружает старую историю — `Message::ChatScrolled { chat, at_top }` эмитится `scrollable::on_scroll` в `chat_thread` и `group_chat` (порог `absolute_offset().y <= SCROLL_AT_TOP_EPS` = 1.0px, устойчив к NaN `relative_offset` когда content≤viewport). `UmApp` получил три поля: `oldest_loaded: HashMap<ChatId,i64>` (keyset-курсор = min id в кэше), `has_more_history: HashMap<ChatId,bool>`, `loading_older: HashSet<ChatId>` (in-flight guard). `note_page` обновляет курсор (min local_id, только если меньше текущего) + has_more. `can_load_older` = has_more && !loading. `ChatScrolled{at_top:true}` → `Command::LoadOlder{peer,before_id}` / `LoadOlderGroup{group,before_id}` только если can_load_older + есть курсор; ставит in-flight маркер. `Event::HistoryLoaded`/`OlderHistoryLoaded` стали struct variants с `has_more`; older-page prepend'ится к кэшу. Bridge: `load_history` грузит newest `HISTORY_PAGE_SIZE=50`, `load_older_history` — следующую страницу; `rows_to_views` общий хелпер. Logout чистит весь pagination-state. 6 новых app-тестов: cursor+has_more на initial page, has_more=false блокирует fetch, ChatScrolled fires LoadOlder + in-flight guard + at_top=false no-op, older page prepend + cursor advance + inflight clear, empty older page всё равно чистит inflight, logout чистит state. 267 тестов зелёных. Выполнено 2026-07-02.
- [ ] GUI: scroll-position preservation при подгрузке старых — сейчас prepend старой страницы к кэшу сдвигает viewport (anchor_bottom + snap_to_end на новых сообщениях не компенсируют рост контента сверху). Нужно сохранить визуальную позицию: запомнить absolute_offset до prepend, после — scroll_to на тот же offset (или на якорь по id верхней ранее-видимой строки). Без этого юзер при подгрузке "прыгает" вниз.
- [ ] GUI: "loading older…" индикатор + "no more history" плейсхолдер — `loading_older`/`has_more_history` уже в state, но view их не показывает. Top-of-thread spinner пока страница в полёте + dim "start of history" когда `has_more=false`.
- [ ] store: `Store::messages` (full-thread) теперь thin wrapper вокруг paged path — можно вырезать прямой ASC-запрос и собирать полный тред пагинацией, либо пометить deprecated и перевести всех callers на `messages_page`. Сейчас оба пути живут параллельно.
